{
  "name": "sporlyworks-microassets",
  "version": "1.0.0",
  "private": true,
  "description": "SporlyWorks OmniSuite — Extension testing & QA",
  "scripts": {
    "test": "npx playwright test",
    "test:ci": "npx playwright test --reporter=github,html",
    "test:shard": "npx playwright test --shard",
    "test:debug": "npx playwright test --debug",
    "lint:manifests": "node tests/helpers/manifest-lint.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
