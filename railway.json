{
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "cd _scripts && gunicorn cws_wave_daemon:app --workers 1 --bind 0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
